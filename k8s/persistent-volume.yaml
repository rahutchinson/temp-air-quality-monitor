apiVersion: v1
kind: PersistentVolume
metadata:
  name: air-quality-pv
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/tmp/air-quality-data"
  persistentVolumeReclaimPolicy: Retain
